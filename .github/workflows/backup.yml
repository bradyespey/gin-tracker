name: Weekly Data Backup (DEPRECATED)

on:
  schedule: [] # Disabled - never runs on schedule
  workflow_dispatch: # Can still be manually triggered, but will fail

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: ⚠️ This workflow is deprecated
        run: |
          echo "❌ This workflow is DEPRECATED and no longer works."
          echo ""
          echo "✅ Please use 'Weekly Data Backup to Google Drive' instead:"
          echo "   1. Go to Actions tab"
          echo "   2. Select 'Weekly Data Backup to Google Drive'"
          echo "   3. Click 'Run workflow'"
          exit 1

